import React from 'react';
import { View, Text,Image} from 'react-native';

class Saved extends React.Component {

  render() {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Saved Code</Text>
        </View>
    );
  }
}

export default Saved;
